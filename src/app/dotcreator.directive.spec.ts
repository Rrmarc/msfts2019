import { DotcreatorDirective } from './dotcreator.directive';

describe('DotcreatorDirective', () => {
  it('should create an instance', () => {
    const directive = new DotcreatorDirective();
    expect(directive).toBeTruthy();
  });
});
